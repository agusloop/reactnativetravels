import React from 'react';
import { Text, View } from 'react-native';

//importar el archivo index o main de src donde contiene las rutas en el contenedor
import AppContainer from "./src/main"

export default () => {
  return <AppContainer />;
}
